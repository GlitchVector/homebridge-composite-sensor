/**
 * Platform alias used in users' config.json under "platform".
 */
export const PLATFORM_NAME = "CompositeSensor";

/**
 * Must match the "name" field in package.json.
 */
export const PLUGIN_NAME = "homebridge-composite-sensor";
